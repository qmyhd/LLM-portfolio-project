#!/bin/bash
# REMOVED: This script is no longer used.
# Use the systemd timer (nightly-pipeline.timer) instead.
echo "This script has been removed. Use the systemd timer (nightly-pipeline.timer)."
exit 1
